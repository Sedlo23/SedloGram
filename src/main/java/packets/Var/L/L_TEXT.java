package packets.Var.L;

import packets.Var.Variables;

public class L_TEXT extends Variables {
    public L_TEXT() {
        super("L_TEXT",
                8,
                "L_TEXT defines the length of a text string (L_TEXT * X_TEXT)");
    }
}
